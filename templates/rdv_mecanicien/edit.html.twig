{% extends 'base_admin.html.twig' %}

{% block title %}Edit Réservation Mécanicien{% endblock %}

{% block content %}
<div class="container">
    <h1>Edit Réservation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Save Changes</button>
    {{ form_end(form) }}

    <a href="{{ path('app_rdv_mecanicien') }}" class="btn btn-secondary mt-3">Back to list</a>
</div>
{% endblock %}